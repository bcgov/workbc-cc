(function ($, Drupal) {
    Drupal.behaviors.workbc = { 
        attach: function (context, settings) {
            //alert($(window).width()+'-'+$('body').width());
            
        }
    }
})(jQuery, Drupal);